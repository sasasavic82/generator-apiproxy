'use strict'

/**
 * @module <%= componentName %>
 * @description Describe the purpose of <%= componentName %>, here.
 * @property {string} attr An attribute.
 */
const <%= componentName %> = {

  attr: 'An attribute',

/**
  * @description A stubbed method that returns a string.
  * @memberof <%= componentName %>
  * @param {string} param - A stubbed parameter.
  * @example
  * fooBar.fn('useful')
  * // => Stubbed method with useful argument
  * @returns {string} A stubbed string with the parameter in it.
  */
  fn: (param) => `Stubbed method with ${param} argument`
}

module.exports = <%= componentName %>
